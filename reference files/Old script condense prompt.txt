f"The PREVIOUS SCRIPT below is too long ({wc} words, max allowed {max_wc} words). "
                    f"Your task is to **EDIT and CONDENSE** the PREVIOUS SCRIPT to fall within the target range, "
                    f"specifically aiming **below {max_wc} words but above {min_wc} words**. "
                    "Achieve this by removing the least essential details, simplifying complex sentences, and "
                    "eliminating all redundant and weak adjectives. "
                    "Preserve all key information and the required structure. " # (Title, Introduction, Overview, Chronology, Parts, "
                    # "Synthesis and Review, Key Names, Closing."
                    "You must return the full, EDITED script only. Do not return the previous script."
                    #f"An LLM just generated the below script, this is intended to be a spoken-word script for an informational podcast."
                    #f"The script is based on the below report."
                    #f"The LLM-generated script has {wc} words, which is above the maximum required word count of {max_wc}."
                    #"Your task is to 1) review the original report, 2) review the previously generated script, 3) review the"
                    #f"script instructions and style example, 3) condense the previous script to stay below the maximum wordcount of {max_wc}."
                    # Note - this step tends to condense too much so just mention max_wc not target_wc.
                    #f"(target {target_wc}, over-margin {over_margin * 100}%). "
                    #"Preserve all key information and the required structure."
                    #"Do not add commentary, just the revised script."